{% extends 'base.html.twig' %}

{% block title %}{{ pagename }}{% endblock %}

{% block body %}
    <h1>Hi There!</h1>

    <div><a href="{{ path('product') }}">New Product</a></div>
    <div><a href="{{ path('sales') }}">Sales</a></div>
{% endblock %}